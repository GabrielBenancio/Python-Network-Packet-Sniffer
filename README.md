# Python-Network-Packet-Sniffer
Real-time packet analysis tool built with Scapy for monitoring and detecting network protocols and traffic patterns.

IF packets don't appear: Try running terminal as Administrator on Windows or using "sudo python packetSniffer.py" in linux as some interfaces require elevated permissions for packet capture
